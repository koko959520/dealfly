/**
 * @deprecated Ce fichier est OBSOLÈTE depuis la migration vers Firestore.
 * Utiliser src/lib/firestore.ts à la place.
 * Ce fichier peut être supprimé.
 */
throw new Error('prisma.ts is obsolete — use firestore.ts instead')
